apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "prueba"
  description: "prueba"
  tags:
    - dotnet
    - csharp
    - webapi
  
  links:
    - url: https://github.com/bpdc-prueba/prueba
      title: Repository
      icon: github

  annotations:
    # this could also be `url:<url>` if the documentation isn't in the same location
    github.com/project-slug: bpdc-prueba/prueba
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: "group:default/team1"
  system: 
